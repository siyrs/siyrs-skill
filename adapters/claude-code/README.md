# Claude Code adapter

The installer reads `commands/*.md` through `scripts/command_registry.py`, installs the six current commands, and removes owned/deprecated adapters from older versions. Re-run it after upgrades.
