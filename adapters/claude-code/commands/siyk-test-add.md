---
description: 识别本轮新增或修改行为，补齐相关测试与回归测试用例并校验。
argument-hint: [quick|standard|strict] [功能范围]
disable-model-invocation: true
---

Use the installed `siyrs-skill` skill. Execute the literal workflow `/siyk-test-add $ARGUMENTS` against the current repository. Establish a trustworthy Git/state baseline, trace changed behavior, author direct and regression test cases, validate them, and persist evidence.
