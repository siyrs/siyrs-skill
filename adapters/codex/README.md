# Codex adapter

Codex discovers one Skill per entry. The installer reads the Markdown registry, installs one core plus six thin explicit-only entrypoints, archives duplicate roots, and archives deprecated entrypoints such as `siyk-test-new` and `siyk-test-full` outside `$HOME/.agents/skills`.
