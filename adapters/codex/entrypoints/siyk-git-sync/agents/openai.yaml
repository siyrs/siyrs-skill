interface:
  display_name: "/siyk-git-sync"
  short_description: "本地提交、集成远程、复验并安全推送当前分支"
  default_prompt: "Run /siyk-git-sync against the current repository."
policy:
  allow_implicit_invocation: false
