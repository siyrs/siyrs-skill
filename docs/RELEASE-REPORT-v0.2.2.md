# siyrs-skill v0.2.2 release report

## Objective

Complete the T1/T2/T3 model across configuration, state, selectors, Git preflight, command registry, adapters, CI, migration, and evidence contracts.

## Delivered

- Markdown command frontmatter registry.
- Config/state schema v2 and state v1 migration.
- Native T2 selector policy and tier-aware templates.
- T1 state-based baseline reuse.
- Git commit/sync preflight composition.
- Exact adapter/CI/install upgrade validation.

## Verification

Unit and contract tests, Python compilation, bundle/release-manifest validation, repository scan, Bash syntax, and Linux installer upgrade/reinstall smoke. Windows install flows are mirrored in GitHub Actions.
