interface:
  display_name: "siyk-git-sync"
  short_description: "Integrate the intended branch and push normally"
  default_prompt: "Use $siyk-git-sync to save intended local changes if needed, integrate the intended remote branch, and push normally."
policy:
  allow_implicit_invocation: false
