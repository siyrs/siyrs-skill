interface:
  display_name: "siyk-git-commit"
  short_description: "保留无关改动并把当前目标变化保存为本地提交"
  default_prompt: "使用 $siyk-git-commit 把当前明确目标改动保存为一个正常本地 Git 提交。"
policy:
  allow_implicit_invocation: false
