interface:
  display_name: "SIYRS Engineering"
  short_description: "Focused repository changes, testing, and Git delivery"
  default_prompt: "Use $siyrs-engineering to make this repository change with a small cohesive diff, proportionate verification, and deliberate Git delivery."
policy:
  allow_implicit_invocation: true
