interface:
  display_name: "SIYRS Skill"
  short_description: "Focused repository changes, testing, and Git delivery"
  default_prompt: "Use $siyrs-skill to make this repository change with a small cohesive diff, proportionate verification, and deliberate Git delivery."
policy:
  allow_implicit_invocation: true
