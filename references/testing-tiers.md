# Test tiers

`test-add` authors tests. T1 is dynamic diff regression. T2 is a fixed native selector. T3 is strict full release gating. T1/T2/T3 do not accept strength arguments. Config validation and plan resolution are authoritative for executable commands.
